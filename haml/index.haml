:plain
  ---
  layout: default
  title: Nicholas Fine
  ---

.posts
  %ul.posts
    :plain
      {% for post in site.posts %}
        <li><span>{{ post.date | date_to_string }}</span> &raquo; <a href="{{ post.url }}">{{ post.title }}</a></li>
      {% endfor %}
